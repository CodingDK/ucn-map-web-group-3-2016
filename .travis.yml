
language: node_js
node_js:
  - "6"
before_install:
  - "cd ucn-map"
  - export PATH="$HOME/.meteor:$PATH"
  - "curl https://install.meteor.com/ | sh"
  - "meteor npm install -g gulp"
before_script:
  - "gulp preparingForSass"
